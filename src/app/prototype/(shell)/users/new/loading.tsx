import { FormPageSkeleton } from '../../../ui/skeleton';

/** Esqueleto del alta de usuario. */
export default function NewUserLoading(): React.ReactElement {
  return <FormPageSkeleton sections={3} />;
}
